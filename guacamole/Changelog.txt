ROM:
No changes

Device:
Fix Bluetooth call bugs from previous build
Fix lags from previous build (QS etc)
Kernel V3.25 state:
- Drop single tap delay change -- needs revision and introduced severe crashes
- Drop input boosting entirely for a more interactive and power efficient configuration
  - device side higher top app min clamp fills in for desired performance and fixes earlier reported "qs lag"
- Improve kprofile performance mode by dropping delay to expedite missed RCU grace periods
