ROM:
Drop possibly buggy duress pin which was in testing.

Device:
Kernel V2.101 state.
fod: remove 10ms post illumination delay. This was an old workaround for hooked dim issues we haven't had for quite awhile.
auto brightness: testing various improvements.
night mode: kang color calibration from pixel.
