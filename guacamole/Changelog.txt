ROM:
Added a toggle for changing the location of the location indicator
Added an ability to turn off gaming macro with the screen
Added a toggle for floating rotation button (Display > auto-rotate)
Added back time spent in app info
Enabled smart auto-rotate
Fixed default animation scale value
Fixed volume dialog loosing tint when animating out
Few other minor stuff

Device:
Updated adreno blobs
Kernel changes:
fixed the idle crashdump for good + others
