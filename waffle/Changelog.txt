ROM:
• None

Device:
• Camera: Enable stock color correction system and lens wide
• Implement a simple UIM2 switch kernel driver and toggle (esim<->physical sim2) for esim variants, which is superior to other solutions in a few ways:
  - A small kernel driver which replaces oplus' without any userspace dependencies
  - Does not have any additional proprietary dependencies
• Kernel: Add kick boost on binder sync tx if from fg main thread to do small boosts on relayout, inflation, etc (the costliest parts of fragment launches)
• Kernel: Merge 6.1.177 and android common kernel
• Kernel: Small additional efficiency improvement
