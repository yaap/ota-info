ROM:
Switch to marlin for gms.persistent to fix CTS.

Device:
  Kernel:
    Implement simple single tap attribute for modernized ST2W sensor.
    Add cmdline interface to check if st2w sensor is used.
    Restore legacy st2w behaviour.

Implement a new and modernized single tap to wake sensor(toggle is located in YASP->Gestures).
